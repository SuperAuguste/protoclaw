syntax = "proto3";

package hello.pog;

import "reverse.proto";
import "otherpkg.proto";

enum Variant {
    Cool = 0;
    Awesome = 1;
}

message Swag {
    Variant variant = 1;
    Reverse reverse = 2;
    .pogpog.ReverseAgain again = 3;
}
